package com.example.tyrepressure

import org.junit.Test
import org.junit.Assert.*

/**
 * Example unit test stub.
 *
 * Unit tests run on the JVM (your development machine), not on an Android device.
 * They are fast and suitable for testing pure logic — e.g. pressure calculations,
 * date formatting, or ViewModel state changes.
 *
 * Run unit tests from Android Studio with: Run → Run 'Tests in com.example.tyrepressure'
 * Or from the terminal: ./gradlew test
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
